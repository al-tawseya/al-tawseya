name: product-search-tests

on:
  push:
    branches:
      - main
      - product-search-engine-v10
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Compile Python
        run: python -m py_compile app.py product_search.py test_product_search.py
      - name: Run search unit tests
        run: python -m unittest -v test_product_search.py
